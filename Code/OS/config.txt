# Force the monitor to HDMI mode so that sound will be sent over HDMI cable
hdmi_drive=2
# Set monitor group to DMT
hdmi_group=2
# Set monitor resolution to 1920x1080 60Hz
hdmi_mode=82
# Set monitor rotation to either 90 degrees clockwise or 270 degrees clockwise (whichever works best)
# 1 for 90, 3 for 270
# display_rotate=
# To get this to work properly, we need to allocate more memory to the gpu (sudo raspi-config). We need to change this from 64M to 128M.